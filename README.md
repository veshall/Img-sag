# This repo accepts a folder of images (can contain nested folder of images also) and filter them with only unique photos in a new folder.
# Uses sharp library to make the hash-code of images thus comparing them effciently.
